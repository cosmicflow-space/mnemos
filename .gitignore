# Dependencies
node_modules/
.pnpm-store/

# Build artifacts
dist/
build/
.next/
out/
*.tsbuildinfo

# Test coverage
coverage/
.nyc_output/

# Environment variables
.env
.env.local
.env.*.local
!.env.example

# Editor / OS
.DS_Store
.vscode/
*.swp
*.swo

# IntelliJ / WebStorm: track project skeleton, drop per-user state.
.idea/*
!.idea/modules.xml
!.idea/*.iml
!.idea/vcs.xml
!.idea/misc.xml
!.idea/codeStyles/
!.idea/inspectionProfiles/
!.idea/runConfigurations/

# Mnemos runtime state (should never be in repo)
.mnemos/
mnemos.db
mnemos.db-*
audit.log
auth.key
encryption.key

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Docker
.dockerignore.local

# pnpm
.pnpm-debug.log

# Caches
.turbo/
.cache/

# Local dev-tooling artifacts (Claude Code, AI partner reviews, etc.)
# The repo-root AGENTS.md / CLAUDE.md symlink is the tracked agent-collab surface.
.claude/

# tesseract.js OCR trained-data (downloaded at runtime; cached under ~/.mnemos/ocr-cache)
*.traineddata

# Local session-handoff scratch (launch planning) — never commit
session-continuation.md
