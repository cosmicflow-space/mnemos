{
  "name": "@mnemos/plugin-codex",
  "version": "0.1.0",
  "description": "Codex ChatProvider for Mnemos — uses your ChatGPT-plan `codex login` session, or CODEX_API_KEY when set.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit" },
  "dependencies": {
    "@openai/codex-sdk": "0.137.0",
    "@mnemos/plugin-sdk": "workspace:*"
  },
  "devDependencies": { "typescript": "^5.6.0" }
}
