import type { Plugin } from "@mnemos/plugin-sdk";

/**
 * @mnemos/plugin-llama-cpp
 * v0.1 scaffold stub. Full implementation lands in next build pass.
 */
const plugin: Plugin = {
  manifest: {
    id: "mnemos-plugin-llama-cpp",
    displayName: "llama-cpp (stub)",
    version: "0.1.0",
    apiVersion: "0.1",
    author: "Mnemos",
  },
};

export default plugin;
